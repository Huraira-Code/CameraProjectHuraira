
import { config } from 'dotenv';
config();

// Intentionally empty.
// Flows are now exposed via Next.js API routes.
// You can start the dev server with `npm run genkit:watch` and `npm run dev`.
// The Genkit UI will be available at http://localhost:4000
